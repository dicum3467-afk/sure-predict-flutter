name: sure_predict
description: Sure Predict football predictions app
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # 🌐 API calls
  http: ^1.2.1

  # ⭐ persist favorites
  shared_preferences: ^2.2.3

  # 🎨 icons (optional but recommended)
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
