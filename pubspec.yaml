name: sure_predict
description: Sure Predict - Football predictions
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  intl: any
  http: ^1.2.2

flutter:
  uses-material-design: true
  assets:
    - lib/l10n/app_ro.arb
    - lib/l10n/app_en.arb
